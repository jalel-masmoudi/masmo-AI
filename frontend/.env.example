# Leave empty for local dev (Vite proxies to http://127.0.0.1:8000)
# Production options:
#   1. Leave empty + deploy to Cloudflare Pages — edge proxy forwards API calls
#      (set BACKEND_API_URL in wrangler.toml / Pages dashboard)
#   2. Set your public FastAPI URL to call the backend directly
# VITE_API_BASE_URL=
